import 'server-only';

/** @deprecated Replaced by aiRuntimeSettings.ts — kept for import stability during transition. */
export { getAiRuntimeSettingsView as getAiProviderSettingsView } from '@/server/ai/aiRuntimeSettings';

/**
 * Public site content types (inferred from Zod — see schema.ts).
 */

export type {
  SiteContent,
  AboutJourneyItem,
  AboutExperienceItem,
  AboutAwardItem,
  DynamicSection,
  DynamicSectionItem,
  DynamicSectionPresentation,
} from './schema';

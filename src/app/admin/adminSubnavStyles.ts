/**
 * Shared admin chrome: compact text links in toolbars (upload, content workflow, etc.).
 * Centralizes the repeated class string for maintainability.
 */
export const ADMIN_SUBNAV_LINK =
  'flex items-center gap-2 text-sm text-muted transition-colors hover:text-foreground';
